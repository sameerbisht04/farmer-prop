{
  "name": "smart-crop-advisory",
  "version": "1.0.0",
  "description": "Smart AI-powered Advisory System for Small and Marginal Farmers",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "pip install -r requirements.txt && cd frontend && npm install"
  },
  "keywords": ["agriculture", "ai", "chatbot", "farmers", "crop-advisory"],
  "author": "Smart Crop Advisory Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
